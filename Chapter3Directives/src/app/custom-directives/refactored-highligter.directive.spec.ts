import { RefactoredHighligterDirective } from './refactored-highligter.directive';

describe('RefactoredHighligterDirective', () => {
  it('should create an instance', () => {
    const directive = new RefactoredHighligterDirective();
    expect(directive).toBeTruthy();
  });
});
