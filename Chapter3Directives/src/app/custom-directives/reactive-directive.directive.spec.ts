import { ReactiveDirectiveDirective } from './reactive-directive.directive';

describe('ReactiveDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactiveDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
