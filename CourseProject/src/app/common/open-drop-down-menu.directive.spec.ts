import { OpenDropDownMenuDirective } from './open-drop-down-menu.directive';

describe('OpenDropDownMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenDropDownMenuDirective();
    expect(directive).toBeTruthy();
  });
});
